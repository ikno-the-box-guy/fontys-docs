---
name: CD

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - DocumentAPI
          - UserAPI

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3.3.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image for ${{ matrix.service }}
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/$(echo "${{ matrix.service }}" | tr '[:upper:]' '[:lower:]'):latest ./${{ matrix.service }}

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: 'docker.io/${{ secrets.DOCKER_USERNAME }}/$(echo "${{ matrix.service }}" | tr "[:upper:]¨ "[:lower:]"):latest'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

      - name: Push Docker image to Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/$(echo "${{ matrix.service }}" | tr '[:upper:]' '[:lower:]'):latest
